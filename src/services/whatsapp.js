const whatsappService = require('../whatsapp');

console.log('--- WHATSAPP SERVICE LOADED (REFACTORED MODULE ARCHITECTURE) at ' + new Date().toLocaleTimeString() + ' ---');

module.exports = whatsappService;
